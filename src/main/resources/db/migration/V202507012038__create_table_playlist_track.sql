CREATE TABLE PLAYLIST_TRACK (
    PLAYLIST_ID UUID NOT NULL,
    TRACK_ID UUID NOT NULL,
    CREATED_DATE TIMESTAMP WITH TIME ZONE DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (PLAYLIST_ID, TRACK_ID)
);

ALTER TABLE PLAYLIST_TRACK ADD CONSTRAINT PLAYLIST_TRACK_PLAYLIST_ID_FK FOREIGN KEY (PLAYLIST_ID) REFERENCES PLAYLISTS(ID) ON DELETE RESTRICT;
ALTER TABLE PLAYLIST_TRACK ADD CONSTRAINT PLAYLIST_TRACK_TRACK_ID_FK FOREIGN KEY (TRACK_ID) REFERENCES TRACKS(ID) ON DELETE RESTRICT;